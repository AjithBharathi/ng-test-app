| Pipe         | Use                       |
| ------------ | ------------------------- |
| `uppercase`  | Text → UPPERCASE          |
| `lowercase`  | Text → lowercase          |
| `titlecase`  | Title Formatting          |
| `slice`      | Substring / Subarray      |
| `date`       | Format Dates              |
| `currency`   | Format Money              |
| `percent`    | Format Percent            |
| `number`     | Format Numbers            |
| `json`       | Object to JSON            |
| `keyvalue`   | Object to key-value pairs |
| `async`      | Observable → value        |
| `i18nPlural` | Text based on count       |
| `i18nSelect` | Text based on selection   |


🆚 Pure vs Impure — Quick Comparison

| Feature                 | Pure Pipe                          | Impure Pipe                           |
| ----------------------- | ---------------------------------- | ------------------------------------- |
| Execution               | When input reference/value changes | On every change detection cycle       |
| Performance             | High                               | Lower (more CPU usage)                |
| Use cases               | Simple transforms (date, string)   | Array filtering, search, dynamic data |
| Handles mutated inputs? | ❌ No                               | ✅ Yes                                |

✅ Summary
Use pure pipes for simple, stateless transforms.
Use impure pipes for dynamic data that might mutate or require frequent updates.